import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  // No need to redefine common styles - they'll be imported from commonStyles
  // Only screen-specific styles here
});