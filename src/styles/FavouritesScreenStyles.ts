import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  // Screen-specific styles only - common styles are imported separately
  // Currently no specific styles needed for FavouritesScreen
});
